# ===========================================
# CertiTrust Production Environment Template
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!

# ===========================================
# Supabase Configuration (Required)
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# ===========================================
# Security Keys (Required)
# ===========================================
# KMS Master Key for encrypting institution private keys
# Generate with: openssl rand -hex 32
KMS_MASTER_KEY=your-32-byte-hex-key

# Legacy issuer private key (optional, for backwards compatibility)
# Generate Ed25519 key with: openssl genpkey -algorithm Ed25519 | base64
ISSUER_PRIVATE_KEY=

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,https://your-domain.com

# ===========================================
# API Configuration
# ===========================================
# Backend API URL (for frontend to call)
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# ===========================================
# AI/ML Configuration (Optional)
# ===========================================
# Hugging Face API token for cloud forensic analysis
HF_TOKEN=

# ===========================================
# Production Settings
# ===========================================
# Number of Gunicorn workers (recommended: 2*CPU + 1)
WORKERS=4

# Log level (debug, info, warning, error)
LOG_LEVEL=info

# ===========================================
# SSL/TLS Configuration (for Nginx)
# ===========================================
# Path to SSL certificate and key
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ===========================================
# Domain Configuration
# ===========================================
DOMAIN=your-domain.com
